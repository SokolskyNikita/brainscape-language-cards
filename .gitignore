# Credentials (at any depth)
.env
.env.*
!.env.example
curl_brainscape.txt

# Local Python environments, packaging and OS files
.venv/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.DS_Store
.pytest_cache/

# Disposable execution output
*.log
*.tmp
*.tmp.*
run.lock

# Production audio is published as GitHub Release assets; keep it out of Git history.
*.mp3
*.wav
*.aiff
*.m4a

# Optional portable audio backup
/cards-audio.tar.gz

# Optional spreadsheet runtime dependencies
node_modules

# Downloaded release archives
/audio/downloads/
